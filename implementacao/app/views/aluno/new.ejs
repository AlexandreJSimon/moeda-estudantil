<!DOCTYPE html>
<html>

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device=width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie-edge">
  <title>Login</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.15.1/css/all.css"
    integrity="sha384-vp86vTRFVJgpjF9jiIGPEEqYqlDwgyBgEF109VFjmqGmIY/Y4HV4d3Gp2irVfcrp" crossorigin="anonymous">
</head>

<body>
  <nav class="navbar navbar-light" style="background-color: #f7dc8b;">
    <div class="container">
      <div>
        <span class="navbar-brand mb-0 h1">Moeda Estudantil</span>
        <a href="/" class="btn btn-secondary">Pagina Inicial</a>
        <a href="/alunos" class="btn btn-light">listagem de aluno</a>
      </div>
    </div>
  </nav>
  <div class="container">
    <div class="pt-5">
      <h2 class="mb-2"><%= typeof aluno === 'undefined' ? 'Cadastro' : 'Edição' %> de Aluno</h2>
      <form action="/aluno/<%= typeof aluno === 'undefined' ? 'new' : 'update' %><%= typeof aluno === 'undefined' ? '' : '/'+aluno.id %>" method="POST">
        <div class="mb-2">
          <label for="email" class="form-label">email</label>
          <input type="email" name="email" class="form-control" id="email" value="<%= typeof aluno === 'undefined' ? '' : user.email %>">
        </div>
        <div class="mb-2">
          <label for="senha" class="form-label">senha</label>
          <input type="senha" name="senha" class="form-control" id="senha" value="<%= typeof aluno === 'undefined' ? '' : user.senha %>">
        </div>
        <div class="mb-2">
          <label for="nome" class="form-label">Nome</label>
          <input type="nome" name="nome" class="form-control" id="nome" value="<%= typeof aluno === 'undefined' ? '' : aluno.nome %>">
        </div>
        <div class="mb-2">
          <label for="cpf" class="form-label">cpf</label>
          <input type="cpf" name="cpf" class="form-control" id="cpf" value="<%= typeof aluno === 'undefined' ? '' : aluno.cpf %>">
        </div>
        <div class="mb-2">
          <label for="rg" class="form-label">rg</label>
          <input type="rg" name="rg" class="form-control" id="rg" value="<%= typeof aluno === 'undefined' ? '' : aluno.rg %>">
        </div>
        <div class="mb-2">
          <label for="instituicaoEnsino" class="form-label">instituicao Ensino</label>
          <input type="instituicaoEnsino" name="instituicaoEnsino" class="form-control" id="instituicaoEnsino" value="<%= typeof aluno === 'undefined' ? '' : aluno.instituicaoEnsino %>">
        </div>
        <div class="mb-2">
          <label for="curso" class="form-label">curso</label>
          <input type="curso" name="curso" class="form-control" id="curso" value="<%= typeof aluno === 'undefined' ? '' : aluno.curso %>">
        </div>
        <div class="mb-2">
          <label for="endereco" class="form-label">endereco</label>
          <input type="endereco" name="endereco" class="form-control" id="endereco" value="<%= typeof aluno === 'undefined' ? '' : aluno.endereco %>">
        </div>
        <button type="submit" class="btn btn-primary mt-3 mb-3"><%= typeof aluno === 'undefined' ? 'Registrar' : 'Editar' %></button>
      </form>
    </div>
  </div>
</body>

</html>