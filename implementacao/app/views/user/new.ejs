<!DOCTYPE html>
<html>

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device=width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie-edge">
  <title>Login</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.15.1/css/all.css"
    integrity="sha384-vp86vTRFVJgpjF9jiIGPEEqYqlDwgyBgEF109VFjmqGmIY/Y4HV4d3Gp2irVfcrp" crossorigin="anonymous">
</head>

<body>
  <div class="container">
    <div class="pt-5">

      <div class="mb-5">
        <a href="/" class="btn btn-outline-success">Voltar para o inicio</a>
      </div>
      <h2 class="mb-5"><%= typeof user === 'undefined' ? 'Cadastro' : 'Edição' %> de Usuário</h2>
      <form action="/user/<%= typeof user === 'undefined' ? 'new' : 'update' %><%= typeof user === 'undefined' ? '' : '/'+user.id %>" method="POST">
        <div class="mb-2">
          <label for="nome" class="form-label">Nome</label>
          <input type="text" name="nome" class="form-control" id="nome" value="<%= typeof user === 'undefined' ? '' : user.nome %>">
        </div>
        <div class="mb-2">
          <label for="email" class="form-label">Email</label>
          <input type="email" name="email" class="form-control" id="email" value="<%= typeof user === 'undefined' ? '' : user.email %>">
        </div>
        <% if (typeof user === 'undefined') { %> 
        <div class="mb-2">
          <label for="senha" class="form-label">Senha</label>
          <input type="password" name="senha" class="form-control" id="senha">
        </div>
        <% } %>
        <div class="mb-2">
          <label for="cpf_cnpj" class="form-label">CPF/CNPJ</label>
          <input type="text" name="cpf_cnpj" class="form-control" id="cpf_cnpj" value="<%= typeof user === 'undefined' ? '' : user.cpf_cnpj %>">
        </div>
        <div class="mb-2">
          <label for="rg" class="form-label">RG</label>
          <input type="text" name="rg" class="form-control" id="rg" value="<%= typeof user === 'undefined' ? '' : user.cpf_cnpj %>">
        </div>
        <div class="mb-2">
          <label for="endereco" class="form-label">Endereço</label>
          <input type="text" name="endereco" class="form-control" id="endereco" value="<%= typeof user === 'undefined' ? '' : user.cpf_cnpj %>">
        </div>
        <div class="mb-2">
          <label for="instituicao_ensino" class="form-label">Instituicao Ensino</label>
          <input type="text" name="instituicao_ensino" class="form-control" id="instituicao_ensino value="<%= typeof user === 'undefined' ? '' : user.cpf_cnpj %>">
        </div>
        <div class="mb-2">
          <label for="curso" class="form-label">Curso</label>
          <input type="text" name="curso" class="form-control" id="curso" value="<%= typeof user === 'undefined' ? '' : user.cpf_cnpj %>">
        </div>
        <div class="mb-2">
          <label for="departamento" class="form-label">Departamento</label>
          <input type="text" name="departamento" class="form-control" id="departamento" value="<%= typeof user === 'undefined' ? '' : user.cpf_cnpj %>">
        </div>
        <div class="mb-2">
          <label for="tipo" class="form-label">Tipo do Usuário</label>
          <select class="form-select" name="tipo" id="tipo" aria-label="Default select example">
            <option selected>Selecione o tipo</option>
            <option value="aluno" <%= typeof user != 'undefined' && user.tipo == 'aluno' ? 'selected' : '' %>>Aluno</option>
            <option value="empresa" <%= typeof user != 'undefined' && user.tipo == 'empresa' ? 'selected' : '' %>>Empresa</option>
          </select>
        </div>
        <button type="submit" class="btn btn-primary mt-3 mb-3"><%= typeof user === 'undefined' ? 'Registrar' : 'Editar' %></button>
      </form>
    </div>
  </div>
</body>

</html>